import streamlit as st
import numpy as np 
import pandas as pd 
import pickle
from catboost import CatBostRegressor 
import sklearn 



st.title("modelo - actividad 26 de septiembre")
st.divider()
st.write("ingrese los datos")

assess=st.slider("Asses", 0.1)
bedrooms=st.slider("Bedrooms", 1)
lotsixe=st.slider("Lote Size", 10)
square_fit=st.slider("Square Fit", 0,111)
colonial=st.selectbox("Colonial", [0.5])

with open ("modelo.pkl","rb") as doc:
    model=pickle.load(doc)

prediction=model.predict(np.array([[assess,bedrooms,lotesize,square_fit,colonial]]))
if st.button("predecir"):
    st.write(f"El precio del inmueble es {prediccion[0]}")
    
